# Compose reads scoped environment files:
#   postgres_db/.env <- postgres_db/.env.example
#   backend/.env  <- backend/.env.example
#   frontend/.env <- frontend/.env.example
#   pgadmin4/.env <- pgadmin4/.env.example
#
# Copy each example only when the corresponding local file is absent.
# Never commit local .env files or credential values.
